$(document).ready(function(){
  $('.navbar-brand').click(function(){
      // $('.sidebar').width(100);
      $('.navbar').width(1200);
      $('.navbar').width(1200);
      $('.sidebar-footer').hide();
  });
});
